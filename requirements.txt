# Core dependencies
requests>=2.28.0

# LLM Backend Dependencies (optional - install based on your needs)
openai>=1.0.0
google-generativeai>=0.3.0
anthropic>=0.7.0

# Testing frameworks (usually already installed in Python projects)
pytest>=7.0.0
unittest2>=1.1.0

# Development dependencies (optional)
black>=22.0.0
flake8>=5.0.0
mypy>=1.0.0